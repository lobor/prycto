query positions($exchangeId: ID!) {
  positions(exchangeId: $exchangeId) {
    _id
    pair
    exchangeId
    available
    locked
    investment
    objectif
    predict {
      up
      down
      predictDate
    }
  }
}
