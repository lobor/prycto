query positions($exchangeId: ID!) {
  positions(exchangeId: $exchangeId) {
    _id
    pair
    exchangeId
    available
    locked
    investment
    exchange
    objectif
    predict {
      up
      down
      predictDate
    }
  }
}
