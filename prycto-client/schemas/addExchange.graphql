mutation addExchange(
  $name: String!
  $exchange: String!
  $publicKey: String!
  $secretKey: String!
) {
  addExchange(
    name: $name
    exchange: $exchange
    publicKey: $publicKey
    secretKey: $secretKey
  ) {
    name
    exchange
    publicKey
    secretKey
    _id
  }
}
