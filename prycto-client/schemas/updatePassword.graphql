mutation updatePassword(
  $oldPassword: String!
  $password: String!
  $confirmPassword: String!
) {
  updatePassword(
    oldPassword: $oldPassword
    password: $password
    confirmPassword: $confirmPassword
  ) {
    _id
  }
}
